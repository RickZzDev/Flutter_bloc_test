name: fs_checklist
description: A flight simulator checklist

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  bloc: ^6.0.0
  flutter_bloc: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  faker:
  mockito:  

flutter:
  uses-material-design: true